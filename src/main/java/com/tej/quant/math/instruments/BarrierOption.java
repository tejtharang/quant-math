package com.tej.quant.math.instruments;

/**
 * Knock-in option - Option that is activated after the asset price hits a particular barrier or else option expires worthless
 * Knock-out option - Option that stays active but gets deactivated after price hits a particular barrier
 */
public class BarrierOption extends Option {
}
